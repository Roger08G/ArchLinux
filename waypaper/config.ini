###############################################################
# 🖼️ WAYPAPER CONFIGURACIÓN
# Autor: Tu setup Hyprland
# Backend: swww
# Tema: Nord
###############################################################

[Settings]
# Ruta a tu carpeta con wallpapers
wallpaper_dir = /home/tuusuario/Pictures/walls

# Programa que cambia el fondo (swww, swaybg, wallutils)
backend = swww

# Duración de la transición (en segundos)
swww_transition_duration = 2.0

# Tipo de transición (fade, grow, outer, random, wipe)
swww_transition_type = fade

# Si quieres un color de fondo previo (por si la imagen no llena toda la pantalla)
background_color = #2E3440

# Si tienes varios monitores, elige “all” o nombres específicos (usa hyprctl monitors)
display = all

# Intervalo en segundos para rotar automáticamente (0 = desactivado)
interval = 0

# Mostrar notificaciones al aplicar fondo (usa libnotify)
notifications = True

# Guardar logs (por si algo falla)
log_level = INFO

# Tema de la interfaz (light, dark, auto)
theme = dark
